# Secrets — never commit
.env
*.env
!.env.example

# Robinhood / robin_stocks pickle session cache
*.pickle
.tokens/
robinhood.pickle

# Geofence SQLite database (data is local-only, never committed)
data/*.db
data/*.db-wal
data/*.db-shm
geofence.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# OS
.DS_Store
