"""Song section — STUB (Phase 2).

Will pick from a curated rotating list in data/songs.json with no-repeat logic.
"""
from __future__ import annotations

from . import SectionResult


def build(history=None) -> SectionResult:
    return SectionResult(title="Song", body="_(song section coming in Phase 2)_")
