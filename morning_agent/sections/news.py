"""News section — STUB (Phase 3).

Will pull top headlines per category from GNews (free tier) and summarise the
top 3-4 in 2-3 sentences.
"""
from __future__ import annotations

from . import SectionResult


def build() -> SectionResult:
    return SectionResult(title="News", body="_(news section coming in Phase 3)_")
